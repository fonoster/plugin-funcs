// 🚀 Let's get started
// Use fonosterfuncs:deploy to publish to the cloud functions
export default (request, response) => {
  console.log("Watch this message with fonosterfuncs:logs");
  return response.succeed("OK");
};
