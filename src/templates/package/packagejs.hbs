{
  "name": "{{ lowerCase pckgName }}",
  "version": "{{ lowerCase pckgVersion }}",
  "author": "{{ author }}",
  "main": "dist/index.js",
  {{#if pckgDesc}}
  "description": "{{ pckgDesc }}",
  {{/if}}
  "scripts": {
    {{#if pckgDesc}}
    "lint": "eslint .",
    {{/if}}
    "deploy": "fonos funcs:deploy",
    "logs": "fonos funcs:logs --follow"
  },
  {{#if eslint}}
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-google": "^0.14.0"
  },
  {{/if}}
  "engines": {
    "node": ">=12"
  },
  "private": true
}

