{
  "name": "{{ lowerCase pckgName }}",
  "version": "{{ lowerCase pckgVersion }}",
  {{#if author}}
  "author": "{{ author }}",
  {{/if}}
  "main": "dist/index.js",
  {{#if pckgDesc}}
  "description": "{{ pckgDesc }}",
  {{/if}}
  "scripts": {
    {{#if eslint}}
    "lint": "eslint .",
    {{/if}}
    "deploy": "fonosterfuncs:deploy",
    "logs": "fonosterfuncs:logs --follow"
  },
  {{#if eslint}}
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-google": "^0.14.0"
  },
  {{/if}}
  "engines": {
    "node": ">=12"
  },
  "private": true
}

