// 🚀 Let's get started
// Use fonos funcs:deploy to send to the cloud functions
module.exports = (request, response) => {
  console.log("Watch this message with fonos funcs:logs");
  return response.succeed("OK");
};
